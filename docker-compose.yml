services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bookhub
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d bookhub"]
      interval: 5s
      timeout: 5s
      retries: 30

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 30

  app:
    build:
      context: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/bookhub?schema=public}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-change-me}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      OTP_SECRET: ${OTP_SECRET:-change-me-too}
      OTP_EXPIRES_MINUTES: ${OTP_EXPIRES_MINUTES:-2}
      OTP_RESEND_COOLDOWN_SECONDS: ${OTP_RESEND_COOLDOWN_SECONDS:-60}
      SMS_PROVIDER: ${SMS_PROVIDER:-}
      SMS_API_KEY: ${SMS_API_KEY:-}
      SMS_TEMPLATE_ID: ${SMS_TEMPLATE_ID:-839779}
      SMS_SENDER: ${SMS_SENDER:-}
      MAX_PROFILE_IMAGE_MB: ${MAX_PROFILE_IMAGE_MB:-2}
      DB_RESET_ON_SCHEMA_CHANGE: ${DB_RESET_ON_SCHEMA_CHANGE:-true}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      TZ: ${TZ:-Asia/Tehran}
    ports:
      - "3000:3000"
    volumes:
      - appmeta:/app/.runtime
      - appuploads:/app/public/uploads

  worker:
    build:
      context: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/bookhub?schema=public}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-change-me}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      OTP_SECRET: ${OTP_SECRET:-change-me-too}
      OTP_EXPIRES_MINUTES: ${OTP_EXPIRES_MINUTES:-2}
      OTP_RESEND_COOLDOWN_SECONDS: ${OTP_RESEND_COOLDOWN_SECONDS:-60}
      SMS_PROVIDER: ${SMS_PROVIDER:-}
      SMS_API_KEY: ${SMS_API_KEY:-}
      SMS_TEMPLATE_ID: ${SMS_TEMPLATE_ID:-839779}
      SMS_SENDER: ${SMS_SENDER:-}
      MAX_PROFILE_IMAGE_MB: ${MAX_PROFILE_IMAGE_MB:-2}
      DB_RESET_ON_SCHEMA_CHANGE: ${DB_RESET_ON_SCHEMA_CHANGE:-true}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      TZ: ${TZ:-Asia/Tehran}
    command: ["npm", "run", "worker:queues"]

  test:
    build:
      context: .
    profiles: ["test"]
    depends_on:
      app:
        condition: service_started
    env_file:
      - .env
    environment:
      E2E_BASE_URL: http://app:3000
    command: ["sh", "-lc", "npm run test:unit && npm run test:e2e"]

volumes:
  pgdata:
  appmeta:
  appuploads:
  redisdata:
